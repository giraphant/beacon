# Beacon

- `native/` — current macOS menu-bar app. Build with `make -C native build`.
- `relay/` — quote relay service used by Beacon.
- `Legacy/` — frozen Raycast extension retained for history. Run its npm commands from that directory.
